# micro_svm.(module-name)

## Overview

### Summary

...
(A brief, compact and to-the-point summary of all sections and subsections below)
(this text would be used for quick cross-reference when working with other modules)
(1 short dense paragraph)

### Purpose

...
(Text that describes module's core purpose on its own)
(This should be the answer to "What can this module do on its own?")
(1-2 short dense paragraphs)

### Role in the Project

...
(Text that describes module's role and relationships with other parts of the project)
(This should be the answer to "Where is this module being used and for what?")
(1-2 short dense paragraphs)

### Contracts

...
(Text that describes expectations and hardcoded assumptions from objects and values coming from other modules if applicable)
(include important information that is specific to this module and is missing/has been omitted in the top-level document)

## Classes
(public interface only)

### `ClassName: BaseClassName1, BaseClassName2, ...`

...
(short dense text describing purpose, role and notable features of this class)
(include important information that is specific to this class and is missing/has been omitted in the top-level document)
(1-2 short dense paragraphs highlighting key details; code snippets are discouraged; hard "file+line number" references are discouraged)

#### Public API

- **Fields:** (only if there are public fields present; in alphabetical order)
  - `public_field_name: type` - ... (short summary describing field's purpose; if field type is a `str`-related collection, try your best to infer what primary/key elements represent)
  - ... (other public fields; private fields are not allowed to be listed here; lambda object/callable is a *field*, not a method)

- **Methods:** (only if there are public methods present; in alphabetical order)
  - `public_method_name (args): return_type` - ... (short summary describing method's purpose, side-effects and overall mechanics)
  - ... (other public methods; private ones shouldn't be listed here)

#### Implementation details

...
(this subsection should **only** be present if this class is **non-trivial** and its implementation **deserves special attention**)
(3-4 short dense sentences highlighting key details; code snippets and hard "file+line number" references are discouraged; use embedded TeX formulas if see fit)

### ... (other publicly-accessible classes)

## Module-level functions
(in alphabetical order; public interface only)

- `function_name (arg: type, ...): return_type` - ... (short summary describing its purpose, side-effects and overall mechanics)
- ... (other public static functions)

---

*AI usage disclosure: this document was generated by a large language model, all text has been validated and edited by a human developer.*
